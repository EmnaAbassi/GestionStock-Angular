import { CommandeFournisseur } from './commande-fournisseur';

describe('CommandeFournisseur', () => {
  it('should create an instance', () => {
    expect(new CommandeFournisseur()).toBeTruthy();
  });
});
